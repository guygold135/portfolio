name: Trigger Live Site Deploy

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Live site auto-sync runs from guygold135/guygold135.github.io every 30 minutes."
